Place your real bottle models here (required):

  Bottle_A.glb
  Bottle_B.glb

Also accepted:
  BottleA.glb / BottleB.glb

The showcase loads each model once and clones them for five fragrances.
Label textures go in /static/textures/
